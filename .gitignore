# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Every environment file stays local. `.env.staging` was previously tracked
# and carried a live Google Maps browser key; real values now come from the
# hosting platform's environment configuration. See .env.example.
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local-only docs (never tracked/pushed)
docs/
